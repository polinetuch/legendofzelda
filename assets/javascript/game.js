$("#linkBtn").attr(value);
$("#miphaBtn").attr(value);
$("#lynelBtn").attr(value);
$("#darkBtn").attr(value);

// function to start the game
// chosen character moves to "Your Character"
// other characters fall into enemies
// when attack button is pressed, both player and enemy's value decrease
function startGame() {}

function restartGame() {}
